apiVersion: v1
kind: Service
metadata:
  name: webui
  namespace: demo
  labels:
    app: webui
spec:
  type: NodePort
  selector:
    app: webui
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30081
